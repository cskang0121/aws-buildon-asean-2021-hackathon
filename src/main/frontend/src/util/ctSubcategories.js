export const CTSUBCATEGORIES = ["Desktop", "Laptops & Notebooks", "MacBook Pro"];

export const ctSubcategoryDropdownOptions = CTSUBCATEGORIES.map((subcategory) => {
  return { value: subcategory, label: subcategory };
});
